import React from "react";
import pageIndicesGenerator from "../utils/pageIndicesGenerator";

const Pagination = ({ storeDataToStore, query, listLength, currentPage }) => {
  return ();
};
export default Pagination;
